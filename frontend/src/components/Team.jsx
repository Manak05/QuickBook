import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
<p>
     Behind every great dining experience is a passionate team that makes it possible. Our chefs craft dishes with love and creativity, while our support staff ensures that your reservations are handled with care and precision. From welcoming you at the door to making sure your evening runs smoothly, our team is dedicated to giving you more than just a meal, we create memories worth sharing.
</p>

        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
